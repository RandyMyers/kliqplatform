/**
 * Email sync cron job - stub implementation.
 * Extend with actual IMAP/email sync logic as needed.
 */
module.exports = {
  scheduleEmailSync() {
    // Placeholder: no-op for now
    console.log('📧 Receiver email sync scheduled (stub)');
  },
};
